
// src/environments/environment.prod.ts  (build --configuration production)
export const environment = {
  production: true,
  apiUrl: '/TrainBook/api'                 // relativo; ver sección ALB
};
